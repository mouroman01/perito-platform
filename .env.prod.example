# Variáveis lidas pelo docker-compose.prod.yml (arquivo deve se chamar ".env" na raiz do projeto)
# Gere senhas fortes antes de subir em produção, por exemplo: openssl rand -base64 24

POSTGRES_PASSWORD=troque-esta-senha-em-producao
MINIO_ROOT_PASSWORD=troque-esta-senha-em-producao
